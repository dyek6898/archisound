const mainTitOptions = {
  revealFirst: {
    initialY: "100%",
    alpha: 0,
    delay: 0,
  },
  revealSecond: {
    initialY: "100%",
    alpha: 0,
    delay: 0.3,
  },
};

const subTitOptions = {
  revealFirst: {
    initialY: "20px",
    alpha: 0,
    delay: 0,
  },

  revealSecond: {
    initialY: "20px",
    alpha: 0,
    delay: 0.3,
  },
};

const contentOptions = {};

export { mainTitOptions, subTitOptions, contentOptions };
